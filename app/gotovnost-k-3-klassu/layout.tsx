export const metadata = {
  title: 'Готов ли ребёнок к 3 классу? — тест с разбором',
  description:
    'Пройдите тест на закрепление базы 2 класса (математика, русский, логика, окружающий мир) и вопросы для родителя о самоорганизации перед переходом в 3 класс. Общий результат — сразу, подробный разбор — по желанию.',
  alternates: { canonical: '/gotovnost-k-3-klassu' },
};

export default function Grade3ReadinessLayout({ children }: { children: React.ReactNode }) {
  return children;
}
